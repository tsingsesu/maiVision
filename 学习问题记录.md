# 学习问题记录（踩坑本）

> **这个文档负责长期记录：学习过程中遇到的问题、踩过的坑、以及"原来如此"的恍然大悟。**
> 格式：问题 → 现象 → 原因 → 正确做法 → 以后的检查习惯。
> 每次踩坑就追加一条，写的时候当作「教两星期后的自己」。

---

## 01 换了模型重训，摄像头实测怎么没提升？

**顺手改了训练脚本，却没改推理脚本的模型路径。**

- **现象**：用 yolov8s（train-3）重训完成后，跑 `detect_oreo.py` 实测奥利奥，识别效果和之前 nano 版相比**没有提升**。
- **原因**：`detect_oreo.py` 第 12 行写死的是旧模型：
  ```python
  model = YOLO(r'runs/detect/train_v5/weights/best.pt')   # nano 版！
  ```
  真正新训练出的 s 模型在 `runs/detect/train-3/weights/best.pt`，但推理脚本一直在读 nano 的权重，所以我测了个寂寞。

- **根因（重要）**：**训练和推理是两条独立的链**：
  - `maitrain.py`（训练）：负责产出 `runs/detect/xxx/weights/best.pt`
  - `detect_oreo.py`（推理）：负责**自己指定**读哪个模型
  - 训练脚本**不会**自动更新推理脚本。改的是训练配置 ≠ 改的是推理配置。

- **正确做法**：推理脚本中把路径改成：
  ```python
  model = YOLO(r'runs/detect/train-3/weights/best.pt')   # yolov8s 版
  ```

- **以后的检查习惯**：每次重训完，**第一件事就是确认推理脚本读的是哪个权重**。排查顺序：
  1. 打开 `detect_oreo.py` 看 yolov8s / train-3 路径
  2. 打开最新 `runs/detect/xxx/weights/` 确认 best.pt 的生成时间
  3. 两个对上，才叫「真的换了模型」

- **补充知识（为什么会自动出现 train-3 这样的名字）**：ultralytics 训练输出目录默认自动编号：上次的 `train/` 被占用就生成 `train2/`、`train-3/`、`train_v5/`……所以**训练输出的目录名不一定是你想要的**，认准 weights/best.pt 就行。

---

（待续……）
