# train-3 训练结果详解（yolov8s · 8/30）

> 这份文档教你**怎么自己看懂** `runs/detect/train-3/` 里的每一个文件。
> 为什么要看训练结果？因为**训练自己不会告诉你好不好**，所有结论都藏在这些数据里。
>
> 对应数据：yolov8s.pt，100 轮计划，实际 71 轮早停，验证集 mAP50=0.995, mAP50-95=0.900

---

## 一、30 秒先说结论

| 问题 | 结论 |
|---|---|
| 训练成功了吗？ | ✅ 成功，正常收敛 |
| 最好的成绩？ | 第 41 轮：mAP50=0.995，mAP50-95=0.90，P=0.99，R=1 |
| 训练健康吗？ | ✅ train 损失一路下降；⚠️ 但验证集只有 5 张图，指标抖动大 |
| 有误检吗？ | ⚠️ 有：验证评估时把背景误检成奥利奥 3 次 |
| 能直接拿比赛吗？ | ❌ 不一定——验证集是官方棚拍图，跟摄像头实测不是一回事 |

---

## 二、文件总览（一图看懂整个文件夹）

| 文件 | 是什么 | 优先级 |
|---|---|---|
| `results.csv` | **成绩单**：每轮训练的全部指标 | ⭐⭐⭐ 必看 |
| `results.png` | 曲线图版：把 CSV 画成图 | ⭐⭐⭐ 必看 |
| `val_batch0_pred.jpg` | **验证集预测效果图**：模型当场答卷 | ⭐⭐⭐ 必看 |
| `words/best.pt` | 真正有用的 **模型本体**（第41轮） | ⭐⭐⭐ 必看 |
| `weights/last.pt` | 最后一轮的模型（第71轮） | ⭐ 存档 |
| `confusion_matrix.png` | 混淆矩阵：错的时候错在哪 | ⭐⭐ 推荐 |
| `confusion_matrix_normalized.png` | 混淆矩阵（百分比版） | ⭐⭐ 推荐 |
| `BoxPR_curve.png` | 精度-召回曲线：**选阈值用** | ⭐⭐ 推荐 |
| `BoxP_curve.png` / `BoxR_curve.png` / `BoxF1_curve.png` | 单个指标 vs 置信度曲线 | ⭐ 补充 |
| `args.yaml` | 训练时用的超参数（谁/在哪跑的记录） | ⭐ 存档 |
| `labels.jpg` | 训练集图画：多少样本、长什么样、都在画面哪里 | ⭐⭐ 暴露数据问题 |
| `train_batch0/1/2.jpg` | 训练时增强后的喂入图（检查用） | ⭐ 检查 |
| `val_batch0_labels.jpg` | 验证集**标准答案**（没有预测框） | ⭐ 对照 |

---

## 三、逐个文件详解

### 1️⃣ results.csv —— 核心成绩单（最重要）

**这是一个表格数据文件，用 Excel 双击打开。**

#### 结构：一行 = 一轮训练

```
列（每列是一个指标）
   │
第1轮 → [epoch=1, train/box_loss=0.78, ..., mAP50=0.86, ...]
第2轮 → [epoch=2, train/box_loss=..., ...]
③ ...  （共 71 行，因为第 71 轮早停了）
```

- **行 = 时间**：第 1 轮模型最笨，越往后越准
- **列 = 指标**：每个指标每轮都会重新测一遍

#### 全部列名对照表

| 列名 | 中文含义 | 怎么看 | 好坏 |
|---|---|---|---|
| `epoch` | 第几轮 | 第 1 行 = 第 1 轮 | — |
| `train/box_loss` | 训练损失：框的位置准不准 | 看趋势：一直降=在学 | 低好 |
| `train/cls_loss` | 训练损失：类别认没认对 | 第1轮 7.96→0.35（前几轮峰值约16，共降约45倍） | 低好 |
| `train/dfl_loss` | 训练损失：框边界质量 | 2.1→1.1，缓慢下降 | 低好 |
| `metrics/precision(B)` | **精确率 P**：模型说「这是奥利奥」的框里，真对的占多少 | 最后轮 0.60~1.0 波动 | 高好 |
| `metrics/recall(B)` | **召回率 R**：该找到的奥利奥里，找到几个 | 后期稳定 1.0 | 高好 |
| `metrics/mAP50(B)` | **mAP50**：主要成绩（重合度 50% 就算对） | 最高 0.995 | 高好 |
| `metrics/mAP50-95(B)` | **mAP50-95**：更严的版本（逐步要求重合度 50%→95%） | **保存 best.pt 就按它** | 高好 |
| `val/box_loss` 等 | 验证集上的损失（不是指标，是辅助参考） | 波动大是正常的 | — |
| `lr/pg0~2` | 学习率（训练时候的油门） | 不用管 | — |

#### 怎么找「最好的那一轮」（Excel 操作）

1. 鼠标点 `mAP50-95(B)` 列任意格子
2. 菜单「数据 → 筛选 → 列标题下拉箭头」
3. 选「降序排列」→ 排到最上面那行就是**最优轮次**
4. 这一行对应的就是 `weights/best.pt`（ultralytics 只按这个指标存 best）

💡 **mAP50 会骗人**：好几轮 mAP50 都是 0.995（都算满分），但 mAP50-95 各不相同（0.68 / 0.72 / 0.90）——严标准才能拉开差距。**看成绩认 mAP50-95 为主，mAP50 为辅。**

#### 本次训练几个关键行（我替你挑出来的）

| epoch | train/cls_loss | P | R | mAP50 | mAP50-95 | 说明 |
|---|---|---|---|---|---|---|
| 1 | 7.96 | 0.83 | 1.0 | 0.86 | 0.73 | 开局居然不差？（预训练权重兜底）|
| 5~10 | 2.3→1.1 | **0** | **0** | **0** | **0** | 突然归零！见「常见问题」|
| 20 | 0.78 | 0.78 | 0.8 | 0.71 | 0.56 | 开始爬坡 |
| 30 | 0.55 | 0.71 | 0.99 | 0.71 | 0.33 | 升掉跌 |
| 40 | 0.49 | 0.95 | 1.0 | 0.995 | 0.80 | 接近起飞 |
| **41** | 0.49 | **0.99** | **1.0** | **0.995** | **0.90** | 👑 **最佳轮次**（best.pt 就是它）|
| 50~60 | 0.41 | 0.82~0.86 | 1.0 | 0.96~0.995 | 0.80~0.86 | 高位震荡 |
| 71 | 0.35 | 0.60 | 1.0 | 0.88 | 0.71 | 最后一轮（P 掉到 0.6 是假象）|

冷知识：**第 41 轮之后到 71 轮，30 轮内没有超过第 41 轮的 mAP50-95 → 触发早停（patience=30）**

---

### 2️⃣ results.png —— 曲线版成绩单（9 宫格）

![results.png 布局]（train-3/results.png）

**九宫坐标：3 行 × 3 列，每格一条曲线**

```
       上排（训练集上的损失）       |       下排（验证集上的结果）
┌─────┬─────┬─────┐
│ box │ cls │ dfl │    ← 训练损失：学会没有
├─────┼─────┼─────┤
│ box │ cls │ dfl │    ← 验证损失：泛化参考
├─────┼─────┼─────┤
│ P   │ R   │mAP50│    ← 验证指标：考多少分
├─────┴─────┴─────┤
│ (第2行第1列)      │
│ mAP50-95        │ ← 最重要：严格成绩
└─────────────────┘
```

#### 每一格怎么读

| 格子 | 名字 | 怎么读 | 本次表现 |
|---|---|---|---|
| 上1 | train/box_loss | **应该一路下降**，降到底=学会定位 | 0.78→0.51 ✅ 健康 |
| 上2 | train/cls_loss | 最陡的一条，下降快=类别很快学会 | 16→0.35 ✅ 健康（45 倍）|
| 上3 | train/dfl_loss | 缓慢下降即可 | 2.1→1.1 ✅ 正常 |
| 下1 | val/box_loss | 参考，**抖动是正常**（5 张验证图水漂太大）| ⚠️ 波动大 |
| 下2 | val/cls_loss | 一直贴地≈分类极自信 | ✅ 无误类 |
| 下3 | val/dfl_loss | 参考 | 波动 |
| 下4 | P（下排第2行第1列）| 精确率，后期在 0.8~1.0 震荡 | ✅ 高位 |
| 下5 | R | 召回率，后期贴 1.0 | ✅ 全检到 |
| 下6 | mAP50 | **最终成绩的主线**，爬到≈1.0 | ✅ 0.995 |
| 下7 | mAP50-95 | 从严成绩，一直在爬 | ✅ 峰值 0.90 |

#### 3 条「健康检查」口诀

1. **train 三条都在往下掉** → 模型学会了（学习正常）
2. **mAP50 爬到接近 1 且不崩** → 结果达标
3. **中间某轮突然归零、又弹回来** → 不是失败，是验证集太小抖毛了（继续看 5️⃣ 的 PR 曲线就知道为什么）

---

### 3️⃣ val_batch0_labels.jpg + val_batch0_pred.jpg —— 模型当场答卷

**这两张是「同一份试卷」，对比着看！**

- `val_batch0_labels.jpg` = **老师版**：验证集 5 张图的**标准答案**（蓝色框是人工标注，没有预测框）
- `val_batch0_pred.jpg` = **学生版**：模型跑完这 5 张后**预测的框**（蓝框 + 类名 + 置信度）

#### 本次 pred 图的逐张解读（5 张全部命中）

| 图片 | 预测结果 | 点评 |
|---|---|---|
| 1.jpg | aoliao_heiqiao 0.9 | 平放正面，全中 |
| 2.jpg | 0.9 | 竖放，侧脸，全中 |
| 3.jpg | 0.8 | 角度刁钻，置信度略低 |
| 4.jpg | 0.9 | 只有侧面「OREO」字，也中了 |
| 5.jpg | 0.9 | 远一点，全中 |

**结论：官方棚拍图 100% 全检到，置信度 0.8~0.9 —— 这就是「val 集上 mAP50=0.995」的画面对应。**

#### 怎么看「预测对不对」的判断方法（通用规则）

- 🔵 蓝框正好套住奥利奥 = ✅ 对
- 🟢 蓝框没套住 = ❌ 框错了（位置错）
- 两个框套一个奥利奥 = ❌ 重复检
- 空的框/框住杂物 = ❌ 误检（把别的东西认成奥利奥）
- 奥利奥没任何框 = ❌ 漏检（模型没看到）

---

### 4️⃣ confusion_matrix.png / confusion_matrix_normalized.png —— 错在哪里

**混淆矩阵：把「预测的结果」和「真正的答案」对表，看错的情况。**

```
            ← 真实的类别（True）
          aoliao_heiqiao    background
预测 aoliao_heiqiao      [ 5 ✅ ]        [ 3 ❌ ]
（Predicted）
预测 background          [ 0 ]
```

- **数字 5**：5 个奥利奥，全对（perfect）
- **数字 3**：模型把背景当成了奥利奥，**误检 3 次**（重点！这就是「误检」的证据）

**归一化版** `confusion_matrix_normalized.png`：把每个格子除以所在列的总数，换成概率（1.00 = 100%）。

| 版本 | 用哪个场景 |
|---|---|
| `confusion_matrix.png`（绝对数量） | 想看具体的错了几次、对了几个 |
| `confusion_matrix_normalized.png`（比例） | 只想比较「谁高谁低」，或者给小伙伴看一个数 |

**怎么看这个对比的结论**：大对角线上数字越大越黑 = 越好；只有 class 列的格子亮起来 = 模型学习正常；如果背景列出现大格子 → 误检严重，考虑调阈值、加背景负样本。

---

### 5️⃣ BoxP / BoxR / BoxF1 / BoxPR 曲线 —— 调阈值工具

这四个图是**同一个数学对象（mAP）的四种画法**，全部给「置信度阈值」当加减用。

| 文件 | 横轴 | 纵轴 | 图的含义 |
|---|---|---|---|
| `BoxP_curve.png` | 置信度 | Precision | 阈值调高 → P 上升 |
| `BoxR_curve.png` | 置信度 | Recall | 阈值调高 → R 下降 |
| `BoxF1_curve.png` | 置信度 | F1（P 和 R 的调和平均） | **最高的点 = 最佳阈值** |
| `BoxPR_curve.png` | Recall | Precision | 画 PR 曲线 |

#### 本次 BoxPR_curve.png（最重要的一张）

![BoxPR 曲线](train-3/BoxPR_curve.png)

- 蓝色线几乎贴着右上角直角边 → **全域 P 和 R 都 ≈1，最好形态**
- 图例写着 `aoliao_heiqiao 0.995` 和 `all classes 0.995 mAP@0.5`
- 曲线下面积（AUC）就是 mAP@0.5，所以面积 0.995 = 全场最优

**为什么这张图值钱？** 它告诉你在置信度 0.5 位置 P=1 R=1，意味着即使不调默认阈值 0.25，正确率也很高；但**你要比赛追求更严就可以看 BoxF1_curve.png 的最高点位置调阈值到那附近**（不过当曲线已经顶到右上角时，调阈值反而收益有限，这点在你的场景下成立——需要实测才知道要不要调）。

---

### 6️⃣ labels.jpg —— 数据为什么有效/无效的解剖图

**这张图不是训练结果！它统计的是「训练集数据长什么样」**，四宫格：

| 位置 | 内容 | 这次的数据显示 |
|---|---|---|
| 左上 | 类别直方图：每个类多少标注框 | aoliao_heiqiao = **185 个框** |
| 右上 | 所有标注框画在同一张图上（框位置概像）| 框**全部集中在画面中央**，没有边缘 |
| 左下 | 标注框中心点 x/y 散点 | x 集中在 0.40~0.55（也是中央）|
| 右下 | 标注框宽高散点 | 大小差异大（0.05~0.6），说明距离变化多 |

**读懂它的 3 个用途：**

1. **能一眼看出你的数据盲区**：奥利奥全在画面中央 → 模型「只见过中央的奥利奥」，边缘的没练过 → 到手扶/拖动到画面边缘时容易漏。**这就是「限制识别区域」方案依据：与其硬扛边缘漏检，不如只在中央区域检测。**
2. **185 vs 30**：30 张原图 × 6 种增强 + 漏网之鱼 ≈ 185 个框，增强没失效，数量够
3. **框大小五颜六色**：0.1~0.6 都有 → 尺寸多样性好，远近距离都没问题

---

### 7️⃣ train_batch0/1/2.jpg —— 增强检查（训前抽查图）

**mosaic 拼接图**：把 16 张图拼成一张大图（augmentation 窗口），画蓝框用 0 标注（0=这类）。

- 怎么看：图被切成 4x4 格子，每格是一张训练图的增强版本（左上角有原图文件名，如 `aoliao_04_3b.jpg`），蓝框 + 数字 0 是标注
- 用途：**肉眼校验增强是否过头**。比如：有哪张图整个变成黑块 → 增强崩了；哪张的增强把奥利奥切掉一半以上 → 可能学不到完整形状
- 本次：增强效果肉眼可见——有水平翻转的（奥利奥方向反了）、有马赛克拼接的（多张图拼一角）、有颜色变化的（变暗/变亮），但奥利奥本体都完整可见，没有严重的「切没了」现象 → 增强设置健康

**记住它的图上是「训练时喂进去的长相」，不是训练结束的结果。**

---

### 8️⃣ weights/best.pt 和 weights/last.pt —— 真正的模型

| 文件 | 是第几轮 | 大小 | 用哪个 |
|---|---|---|---|
| `best.pt` | 第 41 轮（mAP50-95 最高）| 22.5 MB | ✅ **永远用这个** |
| `last.pt` | 第 71 轮（最后一轮）| 22.5 MB | ❌ 存档观察 |

**为什么 best 是 41 轮，不是最后一轮？**
因为 YOLO 训练中 mAP50-95 在第 41 轮达到峰值后就**再也回不去**（后续 cls_loss 还在降，但泛化却变差了 = 开始过拟合训练集）。best.pt 保存的是「验证集（没见过的图）上最准的时刻」。

**怎么用 best.pt：**
- 单个文件测试：`model = YOLO(r'unss/detect/train-3/weights/best.pt')` + `results = model.predict(...)`
- 摄像头/实时识别：在 detect_oreo.py 里加载的路径**改成本路径**即可

---

## 四、三张图告诉你「这个训练健康吗」

```
✅ 训练成功：train/cls_loss 从 7.96 → 0.35（曲线一直下滑）
✅ 得分很高：mAP50 = 0.995，mAP50-95 = 0.90（前 3%）
✅ 分类无痛：confusion_matrix 对角线 100% 全对
⚠️ 数据限制：labels.jpg 显示奥利奥全在画面中央（边缘=盲区）
⚠️ 验证集太小：只有 5 张图 → 你看到的抖动和偶发归零都是它的锅
⚠️ 隐性问题：背景被误检 3 次（confusion matrix 的 background 列）
```

**这些警告怎么应对（对应比赛导向）：**
1. 盲区 → 只检测中央区域（便宜有效）
2. 误检 → 阈值调到 0.7~0.8（上限全中）
3. 抖动 → 多帧投票（连续几帧都检到才输出）

---

## 五、常用问题 QA

**Q1：为什么某几轮 mAP50 = 0，P = 0，R = 0？**
A：这发生在第 5~10 轮，不是模型坏了——是「验证集只有 5 张图」，那几轮在验证集上一张都没检出（置信度全低于 0.25），全部指标就是 0。大训练集不会出现整行归零。

**Q2：为什么 mAP50 到 0.995，还要看 mAP50-95？**
A：0.995 说明「框够粗就算对」100%；0.90 说明「框必须相当细才能算对」90% 达标。mAP50-95 是更严格的考试，**它是决定 best.pt 的指标**。

**Q3：为什么 mAP 这么高，摄像头还是可能不行？**
A：这是经典的「考试题 vs 实战题」：验证集是干净的官方棚拍（5 张固定角度、白底补光），训练集也是差不多；扫地机器人/桌面摄像头是环境光线、手晃、杂物、远近距离——数据分布不一样。**分布偏移 = 你在图书馆练的题，考场变成下午的操场。** 所以 mAP 只是训练场分数，实战分数要摄像头实扫才能见分晓。

**Q4：什么时候建议再看一遍这个文件夹？**
A：每次训练完新模型（比如打算加薯片类）就把这份文档当「体检清单」过一遍：① results.csv 最后几行 ② results.png 两条主线 ③ val_batch0_pred.jpg 逐张看 ④ confusion_matrix 找误检 ⑤ labels.jpg 检查数据盲区。看这一遍，你就是专家。

---

*文档生成：2026-08-30 · 数据来源：runs/detect/train-3（yolov8s.pt 训练共 71 轮 → 早停）*
