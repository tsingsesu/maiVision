# 项目交接文档 — 奥利奥视觉识别（服务机器人比赛·视觉组）

> **给接手对话看**：读完这份，你应该能完全理解这个项目在做什么、做到哪一步、下一步是什么。
> 更新日期：2026-08-30（如后续有进展，请更新此文件）

---

## 一、这是做什么的

**背景**：2025 江苏省机器人大赛 · 服务机器人「人、物辨识赛」。视觉组任务是让机器人用摄像头**识别出桌面上的物品（如奥利奥饼干）并框出来**。

**当前阶段**：个人暑假备赛，从零基础起步，已跑通「**拍图 → 标注 → 训练 → 摄像头实时识别**」全链路，目前只做**奥利奥一个类**（`aoliao_heiqiao`）。

**核心成果**：训练出一个能识别奥利奥的 YOLO 模型，摄像头实时识别可用（置信度约 0.7）。

---

## 二、当前完整资产（都在 `桌面/maiVision/`）

### 代码文件
| 文件 | 作用 | 状态 |
|---|---|---|
| `maitrain.py` | **训练脚本**（当前用 yolov8s） | ✅ 可用 |
| `pipeline.py` | 数据准备流水线（缩放+增强+训练） | ✅ 可用 |
| `pipeline使用方法.md` | pipeline 操作手册 | ✅ 文档 |
| `detect_oreo.py` | **摄像头实时识别奥利奥** | ✅ 用户自建 |
| `cam_detect.py` | COCO 通用检测 demo（认不出奥利奥） | 学习记录 |
| `maivideo.py` | Haar 人脸检测作业 | 学习记录 |
| `maidata.yaml` | 数据配置（路径+类别） | ✅ 当前指向增强集 |
| `maiAnswer.md` | 学习笔记 | 用户自建 |

### 关键数据/模型
- **数据集**：`桌面/yolo_train/`
  - `images/train_aug/` — 180 张增强训练图（30 原图 × 6 增强）
  - `images/train/` — 30 张原始标注图（24 同背景 + 6 新背景）
  - `images/val/` — 5 张官方图（验证集，和训练风格完全不同）
  - `labels/*` — 对应 YOLO 标注 txt
- **模型**：`maiVision/runs/detect/train_v5/weights/best.pt`（nano 版，mAP50=0.995）
  - `runs/detect/train-3/` — 可能是 yolov8s 的最新训练输出（**待确认**）

### Git
- 仓库在 `maiVision/.git`，已有多次提交，最新到 `c77b12d yolov8n->yolov8s`

---

## 三、当前卡在哪 / 正在做什么

**正在做**：把模型从 `yolov8n`（nano）换成 `yolov8s`（small）重训，期望提升对「奥利奥 vs 杂物」的区分度。

**原因（重要背景）**：用 `yolov8n` 训练出的模型在摄像头实测中有问题：
1. 奥利奥置信度只有约 0.7（不够自信）
2. 奥利奥拿在手上大多识别不到，特定角度才行（漏检）
3. 家里杂物背景被误识别成奥利奥（误检）

**诊断结论**：训练在官方棚拍图上 mAP50=0.995（很好），但**摄像头实测场景（手持、杂物、复杂背景）和训练环境（干净背景、固定摆放）差异太大**，导致「分布偏移」。

**当前动作**：已改 `maitrain.py` 用 `yolov8s.pt`，`runs/detect/train-3` 疑似已训练。**下一步需要：确认 train-3 的 mAP、对比 n 和 s 在摄像头上的实际表现。**

---

## 四、接手后要做的事（按优先级）

### 1. 确认 yolov8s 训练结果
- 读 `maiVision/runs/detect/train-3/results.csv`，看最终 `metrics/mAP50(B)` 值
- 和 nano 版（0.995）对比——**注意：验证集分数高不代表摄像头表现好**，要看实测

### 2. 摄像头实测对比
- 用 `detect_oreo.py` 加载 s 模型，在白纸/白布上放奥利奥，1.5~3 米测试
- 看：置信度、漏检、误检是否比 nano 好
- **对比「白底摆拍」（比赛场景）和「手持+杂物」（家用场景）两种效果**

### 3. 比赛导向的优化（重要决策已讨论）
**结论：不需要追求「模型完美」，只需要「比赛条件够用」。**
- 比赛桌面干净、白布、补光 → 误检会少很多
- 三个手段按性价比：
  1. **限制识别区域**（只检测画面中央区域，忽略边缘杂物）— 最简单
  2. 阈值调到 0.7~0.8
  3. 多帧投票（连拍多帧都检到才算）
- **不需要换更贵的芯片**：Jetson Orin Nano 8GB 跑 YOLOv8n/s 绰绰有余（100+ FPS）

### 4. 后续扩展（可选）
- 把薯片、魔爪也做成 2、3 类（流程同奥利奥）
- 写「图片 → 带框结果图」的比赛最终输出脚本

---

## 五、关键知识点（本项目的「血泪」总结）

1. **数据太少 → 过拟合**：30 张不够，用增强扩到 180 张才收敛（`cls_loss` 从 2.5→0.24）
2. **背景单一 → 记背景不记物体**：同一背景拍再多也没用，要换背景
3. **图片尺寸不统一 → 模型学不会**：手机竖拍 4096×3072 vs 官方横图 1280×720，要统一
4. **训练/验证场景差异大 → 分布偏移**：棚拍训练 vs 摄像头实测，效果暴跌
5. **Windows 跑训练必须 `if __name__ == '__main__':`**：否则 DataLoader 多进程报错
6. **OpenCV 坑**：装 label-studio 会连带装 `opencv-python-headless`，抢占 cv2 导致 `imshow` 报 `not implemented`，要卸载 headless 恢复完整版
7. **Label Studio 打开用 `127.0.0.1:8080` 不用 `localhost`**：localhost 解析到 IPv6 会被拒

---

## 六、环境与依赖（接手对话要用）

- **Python 3.13**（`C:\Users\lhl87\AppData\Local\Programs\Python\Python313`）
- **torch 2.11 + cu128**，CUDA 可用，GPU = RTX 5060 Laptop 8GB
- **ultralytics 8.4.116**、**opencv-python 4.10**、**numpy**
- 训练数据在 `C:\Users\lhl87\Desktop\yolo_train`
- 模型在 `C:\Users\lhl87\Desktop\maiVision\runs\detect\...`

---

## 七、给接手对话的提醒

- **用户是零基础，正在学习**：不要直接甩代码，要**讲解原理 + 让他动手**，他重视「为什么」胜过「结果」
- **用户偏好**：喜欢「学什么→有什么用→学会后小车能实现什么」三段式教学
- **用户强调「落地」**：关心代码能不能在真车/真场景跑起来，胜过追求模型指标完美
- **两个分支协作**：此分支负责「理解 maiVision 文件内容」，另一分支负责「写完整输出文本」——**不要两个分支同时改同一个文件**
