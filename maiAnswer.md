## chapter 0
### Q: 为什么要用 read 的返回值判断 “有没有读到帧”？

<mark> A: isOpened()只检测设备是否打开，不能保证每一帧都能读出来，所以用capture.isOpened()不可以</mark>

### Q: 灰度图会用在哪？
<mark>彩色图是 RGB 三通道，一个像素存 3 组数值；灰度图只有单通道亮度值（0 黑～255 白），只保留物体轮廓、明暗纹理，舍弃色彩冗余信息。
公式：Gray = 0.299R + 0.587G + 0.114B（OpenCV 标准）
数据量直接缩减为原图 1/3，对机器人嵌入式主板、笔记本算力压力大幅降低。

<mark>传统人脸检测器（Haar/LBP）只能在灰度图运行。消除衣服、肤色色差干扰，提升人脸检出率。赛场问题：物品重叠、包装五颜六色、桌面纯白背景、干扰杂色多。阈值分割、轮廓提取只能用灰度图（OpenCV 硬性要求）。解决机器人限时运行、算力不足问题（超时直接终止计分）

<mark>边缘检测（Canny/Sobel 找物体边界）、图像二值化（区分前景物品 / 白色背景）等全部依赖灰度图

### Q:cv2.rectangle的四个参数分别是？
<mark> 四个坐标分别是，第一个对角坐标，第二个对角坐标，颜色，粗细

## chapter 1
### Q : x , y , w , h 的对应关系
<mark>x , y 是左上角 原点是图的左上角，往右，x增大，往下，y增大

### Q :如果把 detectMultiScale 的第一个参数 1.1 改成 2.0，会发生什么？
<mark>这是用来检测不同大小的人脸，第一个参数的取值范围是大于1，将1.1改为2.0，它的扫描步长会增加，导致漏检大小不同的人脸（本质是缩小图片进行匹配）

### Q : 把第二个参数5改为1，人脸框会变多还是变少
<mark>会变多。第二个参数minNeighbors = x 一个候选框，周围至少右x个匹配的相邻检测框，才可以判定是一张人脸，所以x越小，越容易错误检测

### Q: 